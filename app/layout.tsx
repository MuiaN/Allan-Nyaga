import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Safari Adventures Kenya | Wildlife Tours & Safari Packages',
  description: 'Experience unforgettable Kenya and Tanzania safaris. Explore Maasai Mara, book luxury accommodations, and discover East African wildlife with expert guides.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
